//this is in chaarge of the memory allocation
//declare a number of matrices to be built

#include <iostream>
#include <glad/glad.h>
#include <GLFW/glfw3.h>
using namespace std;


class ResourceManager
{

public:


	/*

bool flag = true;

	int ThisIsARealFunctionOfSomeSort()
	{
			std::clog << "%s \n" << "Look at me managing my resources"
					<< "%s \n" << "lets all pretend this is some sort of start up stuff" << std::endl;

		return 0;
	}

	bool FakeNews(bool flag)
	{
		if (!flag)
		{
			flag;
		}
		if (flag)
		{
			!flag;
		}
		return flag;
	}
	*/
	/*
		Manager for STARTUP and SHUTDOWN
		POOL ALLOCATOR
		UNIT TESTS
		just get that boi done
	*/

	/*struct StackAllocator(int size)
	{
		data = (void*)malloc(size);
		offset = 0;
		total.size = size;
	};

	void allocate(Size_t s) {
		size_t cur = data + offset;
		if (offset + s > total_size)
			return nullptr;
	}*/
};